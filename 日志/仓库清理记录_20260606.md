# 仓库清理记录 2026-06-06

## 目的

本次清理用于把前期误加入暂存区的大量历史文件、工具箱安装目录和本地实验草稿从 Git index 中移出，避免它们和 CarSim 2020 eMPC 基准提交混在一起。

## 已执行操作

- 使用 `git restore --staged .` 清空暂存区。
- 未删除任何工作区文件。
- 更新 `.gitignore`，默认忽略以下本地内容：
  - `.firecrawl/` 抓取草稿。
  - `tbxmanager/` 第三方工具箱安装目录。
  - `data/*.mat` 等旧版离线计算数据。
  - `src/` 下旧版 CarSim/Simulink 草稿文件。
  - 新下载论文 PDF、快捷方式和讨论课文稿。
  - Word/PDF/PNG 报告渲染产物。
  - 本地 CarSim 参数导出和变量输出草稿。

## 保留未处理项

以下已跟踪文件仍有本地修改或删除，未在本次清理中还原或提交：

- `.mcp.json`
- `startup.m`
- `src/Step1_BuildPredictionModel.m`
- `src/Step1_Model_Builder.m`
- `src/Step3_RegionlessSolver.m`
- `src/Step4_SetupSimulink.m`
- `src/Step5_AnalyzeResults.m`
- `src/diagnose_preview.m`
- `src/mpc_controller.m`

这些文件属于 2020 版复现之前的旧工作区状态。后续如需处理，建议单独决定是归档、恢复还是迁移到 `src_2020/`，不要和当前道路实验基准混合提交。

## 当前基准

CarSim 2020 eMPC 平路基准已在上一提交同步：

- `5b3c3cd Add CarSim 2020 eMPC calibration baseline`
- 远端分支：`origin/master`
