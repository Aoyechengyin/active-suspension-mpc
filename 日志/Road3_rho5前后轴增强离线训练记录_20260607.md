# Road-3 rho5 前后轴增强离线训练记录 2026-06-07

## 背景

上一轮 `ROAD3_SOFT_F1E5_R1E4` 在 T2 50 mm pilot preview 中没有通过 gate：

- `FsExt_*` event 饱和仍约 `15.9%`
- front `Jnc` 最小仍约 `-73.6 mm`
- `Az 0-15 Hz RMS` 没有改善

因此本轮延续“先不改 CarSim 车辆模型，只增强 MPC 控制惩罚”的路线，训练更强 soft profile。

## 历史数据保存

训练前，当前 active root 数据仍为：

```text
ROAD3_SOFT_F1E5_R1E4
front rho5 = 1e-5
rear  rho5 = 1e-4
```

已复制当前 root 的 8 个 MPC `.mat` 到历史快照：

```text
src_2020/road3_rho5_offline/data/history/active_ROAD3_SOFT_F1E5_R1E4_before_F1E4_R3E4_20260607_013504
```

这用于在后续切换失败或 `backup_dir` 丢失时直接回退。

## 新训练 profile

```text
ROAD3_SOFT_F1E4_R3E4
front rho5 = 1e-4
rear  rho5 = 3e-4
```

训练命令：

```matlab
addpath('src_2020');
addpath('src_2020/road3_rho5_offline');

manifest = TrainRoad3Rho5Offline_2020( ...
    'profileName','ROAD3_SOFT_F1E4_R3E4', ...
    'nSamples',8000);
```

输出目录：

```text
src_2020/road3_rho5_offline/data/ROAD3_SOFT_F1E4_R3E4
```

## 训练结果

| Axle | rho5 | Samples | Active sets | Validation max error | Misses | Solver |
|---|---:|---:|---:|---:|---:|---|
| front | `1e-4` | 8000 | 327 | `3.271e-07` | 2 | Gurobi |
| rear | `3e-4` | 8000 | 190 | `5.524e-05` | 2 | Gurobi |

验证结果：

```text
rho5/data check passed:
front_rho5=0.0001
rear_rho5=0.0003
front_sets=327
rear_sets=190
front_err=3.271e-07
rear_err=5.524e-05
```

## 当前状态

本次只完成离线训练和历史数据保存，尚未激活新 profile。

当前 `src_2020` 根目录仍是上一轮 active 数据：

```text
ROAD3_SOFT_F1E5_R1E4
```

原因：训练阶段不应自动覆盖当前仿真入口。等准备跑下一次 T2 pilot preview 时，再显式激活新 profile。

## 激活命令

```matlab
addpath('src_2020');
addpath('src_2020/road3_rho5_offline');

profile_dir = fullfile('src_2020', 'road3_rho5_offline', 'data', 'ROAD3_SOFT_F1E4_R3E4');
backup_dir = ActivateRoad3Rho5Profile_2020(profile_dir);

clear per_corner_mpc_L1_2020 per_corner_mpc_R1_2020 ...
      per_corner_mpc_L2_2020 per_corner_mpc_R2_2020 ...
      per_corner_mpc_state_step_2020
```

## 回退命令

优先使用激活命令返回的 `backup_dir`：

```matlab
RestoreRoad3Rho5Backup_2020(backup_dir);

clear per_corner_mpc_L1_2020 per_corner_mpc_R1_2020 ...
      per_corner_mpc_L2_2020 per_corner_mpc_R2_2020 ...
      per_corner_mpc_state_step_2020
```

如果 `backup_dir` 丢失，可使用本次训练前保存的历史快照：

```matlab
RestoreRoad3Rho5Backup_2020( ...
    fullfile('src_2020', 'road3_rho5_offline', 'data', 'history', ...
    'active_ROAD3_SOFT_F1E5_R1E4_before_F1E4_R3E4_20260607_013504'));
```

## 下一步

激活 `ROAD3_SOFT_F1E4_R3E4` 后，先跑：

```text
Road-3 / T2 50 mm pilot / preview
```

验收重点：

- `FsExt_*` event 饱和比例是否明显低于 `15.9%`
- `FsExt_*` 全程最大饱和比例是否接近或低于 `<1%`
- front `Jnc_L1/R1` 是否从约 `-73.6 mm` 收敛
- `Az_SM` 和 `AA_P` RMS 是否不再恶化
