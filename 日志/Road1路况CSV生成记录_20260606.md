# Road-1 路况 CSV 生成记录 2026-06-06

## 文件

- CSV: `Carsim参数集/3D路况文件/Road1_preview_2mm_raised_cosine_0p16m.csv`

## 目的

Road-1 用于验证 `Zgnd/W1/W2` 的预瞄时间对齐关系，不用于评价控制性能。

## 路面定义

- Station 范围：`0 m` 到 `300 m`
- Station 间距：`0.16 m`
- 数据点数：`1876`
- 列名：`Station(m),elevation(m)`
- 凸包类型：raised-cosine 小凸包
- 凸包起点：`80.00 m`
- 凸包终点：`81.92 m`
- 凸包长度：`1.92 m`
- 峰值位置：`80.96 m`
- 峰值高度：`0.002 m`

公式：

```text
z(s) = 0.5*A*(1 - cos(2*pi*(s - s0)/L))
```

其中：

```text
A  = 0.002 m
s0 = 80.00 m
L  = 1.92 m
```

仅当 `80.00 <= s <= 81.92` 时使用该公式，其余位置高程为 `0`。

## 验证结果

- `StationMin = 0`
- `StationMax = 300`
- `GapMin = 0.16`
- `GapMax = 0.16`
- `ElevMin = 0`
- `ElevMax = 0.002`
- 非零高程点数：`11`

凸包附近关键点：

```text
80.00,0.000000000
80.16,0.000133975
80.32,0.000500000
80.48,0.001000000
80.64,0.001500000
80.80,0.001866025
80.96,0.002000000
81.12,0.001866025
81.28,0.001500000
81.44,0.001000000
81.60,0.000500000
81.76,0.000133975
81.92,0.000000000
```

## CarSim 使用建议

- 使用 CarSim 的“样条插值 + 平线外推法”读取该 CSV。
- Road-1 输出频率建议设为 `1000 Hz`，用于检查 `W1` 约提前 `10 ms`、`W2` 约提前 `20 ms`。
- 不要修改 eMPC 限幅、权重、力方向、Mux 顺序或 CarSim I/O 顺序。
